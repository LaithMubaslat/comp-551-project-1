import lin_reg_algs
import numpy as np
from unittest import TestCase


class Test_gradient_descent(TestCase):

    def test_gradient_descent(self):
        W= np.random.rand(4) #Initialize the vector W randomly
        self.fail()
        # X=Xtrain0wordfeat
        # #Create vector of ones
        # int = np.ones(shape=Y.shape)[..., None]
        # #Add it to feature matrix
        # X = np.concatenate((int, X), 1)
        # Y=Ytrain
        # lin_reg_algs.gradient_descent(X, Y, W, 0.5, 0.5, 1e-7)
